--// ReplicatedStorage.SharedPackages._Index.roblox_roact@1.4.4.roact.internalAssert (ModuleScript)

return function(p1, p2)
    if not p1 then
        error(p2 .. " (This is probably a bug in Roact!)", 3)
    end
end