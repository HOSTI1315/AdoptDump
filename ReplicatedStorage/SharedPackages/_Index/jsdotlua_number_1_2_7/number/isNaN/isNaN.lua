--// ReplicatedStorage.SharedPackages._Index.jsdotlua_number@1.2.7.number.isNaN (ModuleScript)

return function(p1)
    local v2
    if type(p1) == "number" then
        v2 = p1 ~= p1
    else
        v2 = false
    end
    return v2
end