--// ReplicatedStorage.SharedPackages._Index.csqrl_sift@0.0.9.sift.Array.unshift (ModuleScript)

return function(p1, ...)
    local v2 = { ... }
    for _, v3 in ipairs(p1) do
        table.insert(v2, v3)
    end
    return v2
end